# ADN DMR Parrot — minimal PEER config (copy to adn-parrot.yaml; do not commit).
#
# Separate process, same binary as adn-server:
#   python adn-server.py --parrot -c adn-parrot.yaml
#
# Connects to ECHO (MASTER) in adn-server.yaml — match MASTER_PORT and PASSPHRASE.

GLOBAL:
  SERVER_ID: 9990

LOGGER:
  LOG_FILE: /var/log/adn-server/parrot.log
  LOG_LEVEL: INFO

SYSTEMS:
  PARROT:
    MODE: PEER
    IP: 127.0.0.1
    PORT: 54915
    MASTER_IP: 127.0.0.1
    MASTER_PORT: 54917              # = ECHO.PORT in adn-server.yaml
    PASSPHRASE: "<match ECHO.PASSPHRASE>"
    RADIO_ID: 9990
    CALLSIGN: ECHO
    OPTIONS: "TS2=9990;"
